[build]
  command = "npm run deploy:diagnose && npm run build"
  publish = ".next"

# Use Netlify's Next.js runtime (handles SSR, app router, and dynamic routes)
[[plugins]]
  package = "@netlify/plugin-nextjs"

